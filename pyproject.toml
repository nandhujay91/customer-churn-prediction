[project]
name = "customer_churn_prediction"
version = "0.1.0"
description = "Predicts customer churn using historical usage and account data"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]